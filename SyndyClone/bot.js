// SyndyClone Plugin
// Functions
function botURL(user, botname) {
var search = "http://github.com/${user}/${botname}/raw/builds/run.sh"
JSON.prase('http://client.github.com/user/use', login.prase)
exec('git search git+' + search)
return search;
}
// Kareon Main
const pluginURL = { // Give a URL.Plugin Core 13
bot: botURL('Ao', 'Me'),
github: '', // Token Here, go to -> http://token.chvmusic.repl.co to get token
package: ['aoi.js@4.6.4', 'os']
}
return 'Here you go: ' + bot;
